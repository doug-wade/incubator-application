import React from "react"

let Instructions = () => {
  return (
    <div>
      <p className="subtext">
        Applications are due by June 1st, and will not be accepted after that (though the form may still be available). Applications will be reviewed, and the point of contact will be notified by August 1st.  Note that applications are saved in your browser when you close the page, so you must complete the application from only this browser.
      </p>
    </div>
  )
}

export default Instructions;
